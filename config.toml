base_url = "https://giodamelio.com"
title = "Gio's Site"
description = "My random internet thoughts"
author = "Gio d'Amelio"

# Autocompile everything in the sass/ directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# Generate atom feeds
generate_feeds = true

taxonomies = [
    { name = "tags", feed = true},
]

[markdown]
highlight_code = true
highlight_theme = "css"

[extra]
# Put all your custom variables here
