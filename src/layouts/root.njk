<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{% block title %}Home{% endblock %} - Gio d'Amelio</title>
    <link href="/static/css/style.css" rel="stylesheet"/>
  </head>
  <body>
    <nav>
      Gio d'Amelio
      <ol>
        {% for link in navbar.links %}
        <li>
          <a href="{{ link.url }}">{{ link.title }}</a>
        </li>
        {% endfor %}
      </ol>
    </nav>

    <div class="content">
      {{ content | safe }}
    </div>
  </body>
</html>
