---
tabs:
  - direction: Vertical
    parts:
      - run:
          command:
            cmd: just
            args: [zola]
      - run:
          command:
            cmd: just
            args: [tailwind]
